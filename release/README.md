## Template for Release drafts
- ``indexer.js`` Creates an index of the available versions
- ``index.js`` Is an template for the default (latest) update index for the version download environment
- ``publisher.js`` Creates a build.json file, that contains all of the version environment's information

And the ``.json`` files in the ``releases`` directory is an template of how the ``.json`` files should look like.