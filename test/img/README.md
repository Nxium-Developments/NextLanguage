## TEST STRUCTURE 📚
- `default-subnames.img` Uses the default provided hex color subnames.
- `subnames.img` Uses use defined hex code subnames.
- `pure.img` Uses pure hex codes.

This is a test to check if all definitions on the imagery parser is working correctly.